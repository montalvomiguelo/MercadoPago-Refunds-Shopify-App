<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>App</title>
  <link rel="stylesheet" href="https://sdks.shopifycdn.com/polaris/2.10.0/polaris.min.css" />
  <% if ENV['RACK_ENV'] == 'production' %>
    <link rel="stylesheet" href="/packs/application.css">
  <% end %>
  <meta name="ShopOrigin" content="<%= current_shop_url %>" />
  <meta name="APIKey" content="<%= settings.api_key %>" />
  <% if ENV['RACK_ENV'] == 'production' %>
    <meta name="ForceRedirect" content="true" />
  <% end %>
</head>
<body>
  <div id="root"></div>
  <% if ENV['RACK_ENV'] == 'production' %>
    <script src="/packs/application.js"></script>
  <% else %>
    <script src="https://localhost:3035/packs/application.js"></script>
  <% end %>
</body>
</html>
